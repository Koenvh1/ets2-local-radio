name: My workflow

on: push

jobs:
  my-job:
    name: My job
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to foo-bar.surge.sh
        uses: dswistowski/surge-sh-action@v1
        with:
          domain: 'localradio.surge.sh'
          project: 'web'
          login: ${{ secrets.surge_login }}
          token: ${{ secrets.surge_token }}
